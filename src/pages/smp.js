import React from "react"
import Layout from "../components/layout"
import SEO from "../components/seo"

const SMP = (props) => {
    return(
        <Layout>
            <SEO/>

        </Layout>
    );
}

export default SMP;